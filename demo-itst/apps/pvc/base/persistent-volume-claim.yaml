apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-efs-pvc1
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
    storageClassName: efs-csi
    volumeMode: Filesystem
